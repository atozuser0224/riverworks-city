# v0.2 medieval progression and living residents

> 초기 구현을 위한 설계 기록입니다. 현재 동작과 검증 상태는 [아키텍처](ARCHITECTURE.md)와 [검증 기록](VERIFICATION.md)을 확인하세요.

Namespace Riverworks. Existing root owns runtime integration/save/build verification. Do not edit another owner's files. Existing user request: start medieval, tech through eras, visible people actually walk; no clarification needed. Existing game must remain playable, save v1 migration must preserve city.

## Technology core owner
Own Assets/Scripts/Core/GameState.cs, Catalog.cs, Simulation.cs, new Technology.cs; Tests/Program.cs (only core tests; citizen tests will be separate). No other ownership.

Add enums `Era { Medieval, Renaissance, Industrial }`, `TechId { None, CropRotation, Stonecraft, MechanicalPower, Guilds, Metallurgy, Scholarship, Toolmaking, SteamPower, UrbanPlanning }`.
Append BuildingKind enum values `StudyHouse, Academy, SteamPlant` (do not reorder existing values).
GameState v2: `Version=2`; public `Era Era`; `float ResearchPoints`; `List<TechId> Technologies`; `TechId ActiveResearch`; `int ResearchDaysRemaining`; `float LastGrainConsumed`; `bool LastFoodSatisfied`. All serialized defaults safe. Existing fields unchanged.
`TechSpec`: Id, string Name/Description/Benefit, Era Era, int ResearchCost/CoinCost/DurationDays, TechId[] Prerequisites, BuildingKind[] UnlockBuildings.
`TechCatalog.All`, `Get(TechId)`, `EraName(Era)`, `RequiredTechnology(BuildingKind)`, `Has(GameState,TechId)`, `MigrateLegacy(GameState)` (v1 migrates to v2 with all9 technologies and Industrial era to grandfather prior buildings/progression; no file I/O).
`Simulation`: add `bool CanResearch(TechId,out string reason)`, `bool StartResearch(TechId,out string reason)`, `bool IsBuildingUnlocked(BuildingKind,out string reason)` (tech + population gates), public `string ResearchStatus`, `float ResearchProgress`, `float ResearchPerDay`, `int UpgradeLevelCap`.
Research points accrue each day from TownHall and population, more with connected StudyHouse/Academy. One active research at a time, spend points+coins once when starting, complete after days. Failed action cannot mutate. Technology availability enforced in Build and Upgrade, not only UI.
Suggested tree: CropRotation (farm +25%); Stonecraft (quarry, park, level2 upgrades); MechanicalPower needs CropRotation (windmill, mill, bakery); Guilds needs Stonecraft+MechanicalPower (Renaissance, warehouse, market); Metallurgy needs Guilds (mine, smelter); Scholarship needs Guilds (Academy); Toolmaking needs Metallurgy (workshop); SteamPower needs Scholarship+Toolmaking (Industrial, SteamPlant); UrbanPlanning needs SteamPower (level3 upgrades). All9 required before final existing metropolis win, or ensure Industrial era+UrbanPlanning in final check. Balance costs/durations after legal progression test. StudyHouse starts available; basic Road/House/Lumberyard/Farm/TownHall start. New game no seeded windmill. Give enough renewable economy and let residents eat grain as fallback before bakeries (lower happiness / modest growth acceptable), so tech locks cannot starve new city. SteamPlant uses timber as fuel and adds power. No new stock Resource enum (still9). Preserve road bridges.

## Medieval visual owner
Own BuildingVisuals.cs only. `Create(BuildingKind kind,Transform parent,int level=1,Era era=Era.Medieval)` keeps old call compatibility. Full clear medieval, renaissance, industrial art variants for house/townhall and key facilities; add StudyHouse/Academy/SteamPlant. Procedural meshes only; share materials. medieval half-timber, thatch/slate roofs, no modern industrial elements; Renaissance plaster/brick; Industrial brick/chimneys/mechanisms. Keep all existing3D footprint/animation/sound behavior. Root BoardView passes era and redraws on era change. Do not edit BoardView.

## Citizens owner
Own new Core/CitizenSimulation.cs plus new Presentation/CitizenView.cs and Tests/CitizenChecks.cs only. Main test Program root will call CitizenChecks.Run() if requested. No GameState or Simulation modifications.
Pure C# resident engine `CitizenSimulation` consumes GameState. One persistent in-session resident identity per Population with real home assignment, connected job/shop destinations and pathfinding over the actual road/TownHall graph. Plan daily-like staggered routines: home->work->market/park->home, indoor wait states. Streets budget can cap visible residents to ~128 while other residents stay indoors; do not silently change actual population. Replan on demolition/roads/new load. Pause via deltaSeconds=0. No duplicate economic consumption (existing core handles aggregate demand). No walking across water except Road bridges. Roads must remain cardinal contiguous, endpoints building home/work.
Expose enough typed API for meaningful pure C# route/routine tests; send root exact API summary. Public `CitizenView : MonoBehaviour` with `Initialize(GameController)`, `void Refresh()`, `bool TrySelect(Vector2 screenPosition)`, `void ClearSelection()`, `int VisibleCount`, `int ResidentCount`, `string SelectedDetails` (Korean name/job/current destination). Own humanoid model generation/limb walking animation/picking layer9 colliders; root calls TrySelect only selection mode before floor raycast. Pedestrians .27-.34 high, readable clothing caps/cargo, avoid roads center vehicle traffic by sidewalk offset. Drive with controller.GameSpeed and HelpOpen/ModalOpen, rendering matches era. No other Unity process/build. Notify changes via controller.SetNotice only on actual click, not every step.

## HUD owner
Own Hud.cs plus optional new UI/ResearchPanel.cs. Root adds controller `void Research(TechId id)`, `bool ResearchOpen {get;}`, `void ToggleResearch()`, property `string ResidentDetails`, `int PeopleOnStreet`. ResearchOpen pauses/blocks world (root handles), Escape closes. Research panel compact tree/cards9 nodes by era with prereq status, cost, durations, research/currentprogress, start viaResearch. Add top/left era & knowledge button/tab and active research summary; existing population+resource HUD must fit1280x720. Root Changed everyTick refreshes. Locked buildings show required research and cannot selectable even if population high. Upgrade button must respect Sim.UpgradeLevelCap. Default inspector can show street people and click detail when ResidentDetails nonempty. All Korean. Replace old F1 help guidance with medieval food + research instructions. Existing sidebar/controller/modal keys remain. No Unity processes.

## Root responsibilities
Read current APIs and integrate controller/camera/board/save v1->v2 validation/smoke/build/scripts/docs. Board dirt roads beforeRenaissance, stoneRenaissance, asphaltIndustrial; suppress modern vans preIndustrial. Mark era in signatures. Existing file ownership here supersedes old CONTRACT ownership for v0.2.
